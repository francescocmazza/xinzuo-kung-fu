window.XINZUO_ACADEMY_CONFIG = Object.freeze({
  // Filled automatically by the GitHub Pages workflow when the repository
  // variable ACADEMY_API_BASE is configured (for example a Workers custom domain).
  apiBase: "",
  requireAuth: true,
  companyName: "Xinzuo Europe"
});
